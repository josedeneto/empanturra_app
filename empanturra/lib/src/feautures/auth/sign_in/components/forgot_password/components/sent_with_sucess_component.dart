import 'package:flutter/material.dart';

class SentWithSucess extends StatelessWidget {
  const SentWithSucess({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}